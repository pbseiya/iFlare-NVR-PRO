# Modular Explainer Video Clips

This folder contains separate video clips and assets for modular editing.

## Clip 1: Dashboard Flow (Success)
- **Video**: `clip_1_dashboard.mp4`
- **Subtitle**: `clip_1_dashboard.srt`
- **Content**: Intro -> Active Sessions -> Click Cameras link.

## Clip 2: Cameras Grid (Success)
- **Video**: `clip_2_cameras.mp4`
- **Subtitle**: `clip_2_cameras.srt`
- **Content**: Cameras Grid View -> Hover -> Click Recordings link.

## Clip 3: Recordings & Timeline (Use Fallback)
- **Status**: Recording failed (tool timeout).
- **Fallback Image**: `clip_3_fallback.png`
- **Recommendation**: Use this image as a static overlay while narrating the Zoom/Timeline features.
- **Note**: The main `project_explainer_th.mp4` (in parent folder) also covers this section broadly, but without the detailed 5m zoom click.

## Clip 4: Settings (Simple)
- **Status**: Recording failed.
- **Recommendation**: Use the main `project_explainer_th.mp4` for this part, as it successfully captured the theme toggle at the end. Or simply record a 5-second clip of toggling the theme yourself if needed.
